## Linear Workflow Integration

This project uses Linear for issue tracking with MCP (Model Context Protocol) integration. This enables Claude Code to read, create, update, and comment on Linear issues directly.

### Team Configuration

- **Team:** {{linear.teamName}} ({{linear.teamKey}})
- **Workspace:** {{linear.workspaceName}}
- **Issue Pattern:** {{formats.issuePattern}} (e.g., {{formats.issueExample}})

### Branch Strategy

- **Primary Branch:** `{{branches.main}}`{{#branches.staging}}
- **Staging Branch:** `{{branches.staging}}`{{/branches.staging}}{{#branches.prod}}
- **Production Branch:** `{{branches.prod}}`{{/branches.prod}}

### Status Transitions

| Event | Linear Status |
|-------|---------------|
| Push to feature branch | {{linear.statuses.inProgress}} |{{#branches.staging}}
| PR merged to {{branches.staging}} | {{linear.statuses.staging}} |{{/branches.staging}}
| PR merged to {{branches.main}} | {{linear.statuses.review}} |{{#branches.prod}}
| PR merged to {{branches.prod}} | {{linear.statuses.done}} |{{/branches.prod}}

### Quick Workflow

**Starting work on an issue:**
```
User: "Start work on {{formats.issueExample}}"

You should:
1. git checkout {{branches.main}} && git pull origin {{branches.main}}
2. get_issue("{{formats.issueExample}}") + list_comments("{{formats.issueExample}}")
3. Create {{paths.issues}}{{formats.issueExample}}-brief-desc/ folder
4. Reformat issue (11-section format) → task-analysis.md
5. Write deep technical analysis in same file
6. create_comment("{{formats.issueExample}}", "## Task Analysis Complete\n...")
7. git checkout -b feature/{{formats.issueExample}}-brief-desc
8. git commit --allow-empty -m "chore: Initialize {{formats.issueExample}} with task analysis..."
9. git push -u origin feature/{{formats.issueExample}}-brief-desc
```

**Creating a pull request:**
```
User: "Create PR for {{formats.issueExample}}"

You should:
1. gh pr create --title "{{formats.issueExample}}: Description" --body "..."
2. create_comment to post PR summary
3. Issue status updates automatically via GitHub Actions
```

### Commit Format

```
<type>: <description> ({{formats.issueExample}})

Optional detailed description...

Related: {{formats.issueExample}}

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### Full Documentation

See `docs/linear-workflow.md` for complete workflow guide.

---

**Configuration:** `.linear-workflow.json`
**Generated by:** claude-linear-gh-starter
