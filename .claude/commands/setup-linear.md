# Setup Linear + Claude + GitHub Workflow

Execute the complete Linear workflow setup wizard for this project.

This will:
- Run environment checks (git, GitHub CLI, Node.js)
- Fix authentication issues automatically
- Configure Linear workspace and team
- Set up branch strategy and status mappings
- Install GitHub Actions workflow
- Create Linear issue templates
- Set up git hooks and documentation
- Create a test issue to verify the setup

The wizard takes approximately 5 minutes and will guide you through all configuration options.

Start the interactive setup wizard now.
